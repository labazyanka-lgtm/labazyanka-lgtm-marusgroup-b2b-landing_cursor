# Agents

## Cursor Cloud specific instructions

This repository ("Marusgroup B2B Landing") is currently an empty project stub containing only a `README.md`. There are no source files, dependencies, build scripts, or services to run.

**Current state (as of initial setup):**
- No application code, no package manager lockfiles, no dependency manifests.
- No lint, test, or build commands available.
- No services to start or external dependencies required.

When source code and a build system are added, this section should be updated with:
- How to install dependencies (e.g., `npm install`, `pnpm install`).
- How to run the dev server, lint, test, and build.
- Any required environment variables or secrets.
- Any non-obvious caveats discovered during setup.
